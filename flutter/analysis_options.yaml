include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    invalid_annotation_target: ignore # freezed/json_serializable بيسببوا false positive معروف
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.config.dart"
    - "lib/l10n/**"

linter:
  rules:
    # --- Correctness / Production Safety ---
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_print: true # استخدم core/utils/logger بدل print في كل مكان
    avoid_returning_null_for_void: true
    cancel_subscriptions: true
    close_sinks: true
    no_logic_in_create_state: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true

    # --- Clean Code / Style ---
    always_put_required_named_parameters_first: true
    avoid_empty_else: true
    avoid_redundant_argument_values: true
    avoid_unnecessary_containers: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_child_properties_last: true
    sort_constructors_first: true
    use_key_in_widget_constructors: true

    # --- Architecture discipline ---
    depend_on_referenced_packages: true
    directives_ordering: true
    package_api_docs: false # مش مطلوب توثيق API عام لمشروع تطبيق (مش package)
