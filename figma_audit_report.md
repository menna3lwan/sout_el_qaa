# تقرير المراجعة الشاملة ومزامنة التصميم (Figma Full Audit & Sync)
**تطبيق: صوت القاع (Sout El-Qaa) — Flutter**
**تاريخ الجلسة:** 27–28 أغسطس 2026
**ملف Figma:** صوت القاع (`ysvQxQut5Yu72tKp5wp3HA`)

---

## 1. نظرة عامة

المهمة كانت مراجعة شاملة للتطبيق الحالي مقابل أحدث نسخة من تصميم Figma (بعد إعادة التصميم اللي حصلت بعد ما جزء من التطبيق كان اتبنى فعلاً)، وتطبيق كل الفروقات الحقيقية بدون كسر أي وظيفة موجودة أو تغيير في البنية المعمارية (Clean Architecture / Cubit / DI / Repository Pattern).

المنهجية المتبعة: **إعادة جلب Figma بشكل طازج لكل شاشة** (مش الاعتماد على ملاحظات قديمة)، لأن جزء من الشغل اللي كان اتعمل في نفس الجلسة قبل كده طلع فيه أخطاء بسبب الاعتماد على ملاحظات سابقة بدل الرجوع للمصدر الحقيقي. الشاشات اللي اتراجعت بالكامل بهذا الأسلوب: **الرئيسية (Home)، قائمة الشكاوى (Complaints List)، تفاصيل الشكوى (Complaint Details)، الملف الشخصي (Profile) جزئيًا، الإشعارات (Notifications)، وBottomNavBar المشترك**.

---

## 2. التغييرات المُطبَّقة من Figma حسب الشاشة

### 2.1 الرئيسية (Home)
- عناوين الأقسام ("أكثر الشكاوى تفاعلاً"، "تصنيفات الشكاوى") كانت بخط/حجم غلط (نتيجة عمل سابق في نفس الجلسة اتكشف بإعادة الفحص) — رجّعتها لـ`sectionHeadingLarge` الجديد المطابق تمامًا لعقدة Figma.
- زر "عرض الكل" كان بلون وخط قديمين (`metaText` + لون navy مختلف) — دلوقتي `linkButtonBold` (Cairo Bold 14px، اللون الصحيح `textFigmaPrimary`).
- ارتفاع صف التصنيفات اتصحح من 96 إلى 68 ليطابق حجم الأيقونة الدائرية الجديدة (48×48) بدل التصميم القديم.
- عنصر "نشاطاتك الأخيرة/شكاوى محتاجة صوتك" كان بيستخدم `ComplaintListCard` الكبيرة، والتصميم الجديد بيطلب صف مبسّط (pill بيضاء 56px) — اتبني widget جديد `_RecentActivityItem` مخصوص لده.
- `CategoryChip` اتعاد بناؤه بالكامل: دائرة 48×48 للأيقونة + label منفصل تحتها (بدل مستطيل واحد يحيط بالاتنين).

### 2.2 قائمة الشكاوى (Complaints List)
- صف "المشاهدات/الإعجابات" في كارت الشكوى كان ظاهر دايمًا؛ فحص Figma الطازج أكد إنه مش موجود في التصميم الجديد لا في القائمة ولا في كارت الرئيسية — بقى اختياري (`showEngagementStats`, افتراضيًا `false` في كل مكان، لحد ما شاشة تانية تأكد احتياجها له).
- سمك حدود الكارت 1.5px → 2px، والصورة المصغّرة 72×72 من غير حدود → 80×84 مع حد 1px، ونص الوقت بقى بخط أصغر (`stepLabel` بدل `metaText`) مطابقةً للعقدة الحقيقية.
- ترتيب تابات الفلتر ("كل الشكاوى/شكوائي/تم حلها") كان بترتيب مخالف لترتيب Figma البصري — اتصحح لـ(تم حلها، شكوائي، كل الشكاوى) مع الحفاظ على قيم الـenum الأصلية زي ما هي (تغيير عرض بس، مش تغيير في منطق العمل).
- الـcomponent المشترك `FilterPillTabs` (مستخدم في القائمة والإشعارات) اتوسّع بـ6 خصائص اختيارية لألوان الحالة المختارة/غير المختارة، بدل ما تفرض قائمة الشكاوى شكلها على الإشعارات.
- إصلاح مخالفة RTL في `ComplaintStatusStepper` (كان بيستخدم `left:` صريح بدل `EdgeInsetsDirectional`).

### 2.3 تفاصيل الشكوى (Complaint Details)
- عنوان الشكوى كان بخط 20px SemiBold، والفحص الطازج أظهر إنه فعليًا 16px Bold — اتصحح.
- شارة "عاجل" (للشكاوى عالية الخطورة) كانت غير موجودة في هذه الشاشة رغم وجودها في Figma — اتضافت (بلون `urgentBadgeAltDetailPage` المختلف عن شارة الرئيسية، ده كان موثّق مسبقًا كفرق حقيقي).
- شارة الحالة (`StatusBadge`) المكررة بجانب العنوان اتشالت لأن Figma الحالي مابيوريهاش هنا — الحالة بتتوصّل بس من الـstepper تحت.
- خط عنوان القسم "التفاصيل" كان بخط عائلة تانية (Baloo بدل Cairo) — اتصحح لـ`cardTitle` (اللي كان أصلاً موثّق إنه المفروض يُستخدم هنا، بس ماكانش مطبّق).
- كارت الوصف كان بخلفية رمادية فاتحة بدون حدود، والتصميم الحقيقي أبيض بحد 2px — اتصحح.
- ارتفاع الخط الواصل في الـstepper كان 4px بدل 8px الحقيقي — اتصحح.

### 2.4 الملف الشخصي (Profile) — جزئي
- عناصر قائمة الإعدادات (المعلومات الشخصية/شكاواي/المفضلة/الإعدادات/تسجيل الخروج) كانت بأيقونات بدون خلفية دائرية؛ Figma بيوري كل أيقونة جوه دائرة 40px بلون مميز — اتضافت (توكنز لونية جديدة `settingsIconCircleBackground` / `...Destructive`).
- **لم يتم تطبيق:** نظام "الرتب والنقاط" الجديد بالكامل (شارة رتبة "منقذ بحري" فوق الصورة، شريط تقدّم للمستوى، إعادة تصميم كروت الإحصائيات بألوان مختلفة لكل خانة) — التفاصيل والسبب في قسم "المسائل المتبقية" تحت.

### 2.5 الإشعارات (Notifications)
- ترتيب تابات الفلتر ("الكل/شكاوى/تفاعلات/عام") كان بترتيب معكوس تمامًا لترتيب Figma — اتصحح لـ(عام، تفاعلات، شكاوى، الكل).
- ألوان تابات الفلتر (المختارة وغير المختارة) كانت بتاخد قيم افتراضية من `FilterPillTabs` مصمَّمة أصلاً لقائمة الشكاوى، مش القيم الحقيقية لشاشة الإشعارات — اتصححت الاتنين (bg شفاف أبيض + حد كحلي غامق + نص رمادي مختلف لغير المختار، وذهبي مصمت بدون حدود للمختار).
- سمك حدود كارت الإشعار 1.5px → 2px، وعنوان الكارت كان بخط `fieldLabel` (14px) بدل الحجم الحقيقي 18px Bold — اتصحح (توكن جديد `notificationCardTitle`).
- ألوان شارات الأيقونات (أخضر/أصفر/أخضر فاتح/أزرق) طلعت **مطابقة تمامًا** بالفعل للقيم الحقيقية — مافيش تغيير هناك.

### 2.6 BottomNavBar المشترك (كل الشاشات)
- الشريط السفلي كان بشكل الـtheme الافتراضي المسطّح؛ Figma بيوريه بزوايا علوية دائرية 32px، خلفية رمادية فاتحة، وحد علوي ذهبي 4px — القيم التلاتة دي كانت **موجودة أصلاً كـtokens موثّقة** (`surfaceLightGrey`, `navyBarAccentBorder`, `radiusXl`) بس مش مطبّقة في الـwidget — اتوصلت دلوقتي.
- دائرة زر "إضافة" المرتفع كانت بلون الـtheme العام بدل `AppColors.fabBackground` (توكن موجود من قبل وموثّق بالظبط لهذا الاستخدام، بس ماكانش مستخدَم) — اتصحح مع إضافة حد وظل مطابقين لـFigma.

---

## 3. مراجعة الملاحة (Navigation Audit)

- تدفق "الإشعارات → إشعار → الشكوى المرتبطة" شغّال زي المطلوب (`context.push(RoutePaths.complaintDetails(...))`).
- تدفق "قائمة الشكاوى → تفاصيل الشكوى" شغّال بنفس الطريقة.
- تاب "إضافة" في الشريط السفلي معمول كـ`push` مش branch خامس — قرار معماري موثّق من جلسة سابقة (`[P14]`)، اتأكد إنه لسه منطقي مع التصميم الجديد ولم يتغيّر.
- مفيش أي تغييرات مطلوبة في `route_paths.dart` أو بنية الـrouter نفسها.

---

## 4. مراجعة الحزم (Packages Audit)

**لم تتم إضافة أي حزمة جديدة في هذه الجلسة.** `pubspec.yaml` لم يُعدَّل إطلاقًا. الاعتماد الوحيد المذكور كـ"مقترح" (`google_fonts`) كان موجودًا من قبل هذه الجلسة وما اتلمسش.

---

## 5. جودة الكود والتنقيح (Code Quality)

- كل الإصلاحات اتعملت **داخل نفس البنية المعمارية الموجودة** (Cubit/Repository/DI) — مافيش أي منطق عمل جديد تم وضعه داخل الـwidgets.
- تفضيل تصحيح الـtoken الموجود في مكانه (لما يكون له استخدام حقيقي واحد بس) بدل تكرار توكن جديد بجانبه — تطبيقًا لقاعدة "متكررش المكونات".
- كل توكن لوني/طباعي جديد اتضاف موثّق بتعليق يذكر رقم عقدة Figma والسبب، ومعلَّم بـ`[Requires Confirmation]` لو القيمة فيها شك حقيقي (مش مجرد اختلاف بسيط).
- تم تمديد `FilterPillTabs` بمعاملات اختيارية بدل تكرار الكومبوننت أو فرض شكل شاشة على التانية — نفس نمط "opt-in per call site" المستخدم أصلاً في `ComplaintListCard`.
- تم إصلاح 5 مخالفات RTL (استخدام `left:`/`right:` صريح بدل `EdgeInsetsDirectional`/`PositionedDirectional`) في: `create_complaint_page.dart` (×2)، `complaint_list_card.dart` (×2)، `status_badge.dart` (×1).
- تم إصلاح مخالفة typing (`dynamic` بدل النوع الحقيقي `Category`) في `create_complaint_page.dart`.
- تم حذف parameter ميت (`state` في `_FormNavButton`) لم يكن يُستخدم فعليًا.

---

## 6. نتائج التحقق (Verification)

### فحص ثابت (Static)
**تعذّر تشغيل `flutter analyze` و`flutter test` فعليًا في بيئة الجلسة هذه** — السبب تقني وليس تجاهلًا: أداة `device_bash` تعمل داخل جهاز افتراضي منفصل تمامًا عن جهاز الماك الحقيقي (لا يوجد Flutter SDK ظاهر هناك)، وبيئة السحابة الأخرى تقدر تعمل `git clone` لـFlutter SDK لكن تحميل Dart SDK نفسه (من `storage.googleapis.com`) محجوب بجدار الشبكة (خطأ 403). بديلاً عن ذلك، تمت **مراجعة يدوية دقيقة** لكل ملف قبل رفعه: التأكد من صحة الـimports، مطابقة توقيعات الـwidgets الحقيقية، فحص كل استخدام لـ`EdgeInsetsDirectional`/`PositionedDirectional`، وعمل `grep` شامل بعد كل تعديل للتأكد من عدم بقاء أي `left:`/`right:` صريح.

**التوصية:** بعد سحب هذه التعديلات، شغّلي محليًا:
```
flutter pub get
flutter analyze
flutter test
```

### فحص وقت التشغيل (Runtime) والفحص البصري (Visual)
**لم يتم تشغيل التطبيق فعليًا في هذه الجلسة** — لا توجد وسيلة لتشغيل محاكي/جهاز Flutter داخل بيئة العمل الحالية. كل عمليات المطابقة تمت بمقارنة الكود الناتج مباشرة مع رد `get_design_context`/`get_screenshot` من Figma (القيم اللونية، الأحجام، المسافات) سطرًا بسطر، وليس بمقارنة صور فعلية من تطبيق شغّال.

**التوصية:** بعد تشغيل التطبيق محليًا، قارني الشاشات الخمس المذكورة مع لقطات الشاشة المرفقة من Figma (راجعي القسم 8 تحت).

---

## 7. المسائل المتبقية (Remaining Issues)

| # | الشاشة | الوصف | السبب |
|---|--------|-------|-------|
| 1 | الملف الشخصي | نظام رتب/نقاط كامل جديد في Figma (رتبة "منقذ بحري"، شريط تقدّم %49 للترقية، كروت إحصائيات بألوان مختلفة لكل خانة) غير مُطبَّق | يحتاج بيانات domain جديدة كليًا (rank، نسبة المستوى، عتبة الترقية القادمة) غير موجودة في `ProfileStats` الحالي؛ تعليق موجود من جلسة سابقة في `stat_card.dart` يشير بالفعل لاحتمال إن المحتوى النصي ("منقذ بحري"، 245 فقاعة) تجريبي مش نهائي — يحتاج تأكيد من فريق المنتج/التصميم قبل بناء الـbackend المطلوب له |
| 2 | تفاصيل الشكوى | صف شارات (درجة الخطورة/التصنيف/الموقع) بجانب العنوان — 3 pills بألوان وأيقونات مختلفة (مثال: "كارثة قومية") | يحتاج تصنيف خطورة أوسع من `ComplaintSeverity` الحالي (high/medium/low)؛ إضافة تصنيف "كارثة قومية" كقيمة عرض منفصلة يحتاج قرار منتج |
| 3 | تفاصيل الشكوى | صف تفاعلات علوي بـ3 عدادات (تقرير/عدم إعجاب/إعجاب) بدل الإعجاب+المشاهدات الحاليين | يحتاج حقول domain جديدة (تقارير، عدم إعجاب) غير موجودة حاليًا في `Complaint` |
| 4 | تفاصيل الشكوى | إعادة تصميم كارت التعليق (صورة مُبلِّغ 48px بحد بني مخصص، صندوق إدخال تعليق بزر إرسال ملوّن) | تحسين بصري إضافي غير حرج، مؤجَّل لصالح إنهاء باقي الشاشات ضمن وقت الجلسة |
| 5 | BottomNavBar + الإشعارات | أيقونات Figma الأصلية إيموجي/SVG مخصصة (🦑👍🛡️🐌 في الإشعارات، أيقونات مخصصة في الشريط السفلي) بدل Material Icons الحالية | تعليقات موثّقة من جلسات سابقة تشير لاحتمال إنها محتوى placeholder غير نهائي في Figma؛ الأيقونات الحالية بديل دلالي معقول ومُتفق عليه سابقًا |
| 6 | قائمة الشكاوى/الرئيسية | `ComplaintListCard.showEngagementStats` أضيف كخيار لكن لا توجد شاشة مؤكدة تحتاجه بعد (على الأرجح "شكاواي/My Complaints") | لم تتم مراجعة شاشة "شكاواي" بعمق كافٍ لتأكيد الحاجة |
| 7 | عام | 7 قيم navy و5 قيم أصفر متقاربة جدًا عبر `app_colors.dart` (من جلسات سابقة) لسه محتاجة تأكيد من المصمم: تنويع مقصود ولا drift في ملف Figma | يحتاج تواصل مباشر مع فريق التصميم، مش قرار تقني |
| 8 | عام | التحقق الثابت (`flutter analyze`/`test`) والتحقق البصري/وقت التشغيل لم يتما فعليًا هذه الجلسة | قيود بيئة العمل التقنية (موضحة في القسم 6) |

---

## 8. لقطات الشاشة (Figma Reference)

نظرًا لتعذّر تشغيل التطبيق فعليًا، اللقطات المرفقة هنا هي **من Figma فقط** (تم جلبها طازجة أثناء هذه الجلسة) كمرجع للمقارنة اليدوية بعد تشغيل التطبيق محليًا:
- قائمة الشكاوى (عقدة 33:663)
- تفاصيل الشكوى (عقدة 33:518)
- الملف الشخصي (عقدة 33:794)
- الإشعارات (عقدة 33:936)

*(اللقطات نفسها ظهرت أثناء الجلسة عبر أداة Figma MCP ولم يتم حفظها كملفات منفصلة؛ لو محتاجة نسخة منها كملفات صور، أقدر أجيبها تاني بسهولة.)*

---

## 9. الملفات المعدَّلة في هذه الجلسة (Complaints List → BottomNavBar)

```
flutter/lib/core/theme/app_colors.dart
flutter/lib/core/theme/app_typography.dart
flutter/lib/core/widgets/bottom_nav_shell.dart
flutter/lib/core/widgets/filter_pill_tabs.dart
flutter/lib/core/widgets/settings_menu_item.dart
flutter/lib/features/complaints/presentation/pages/complaint_details_page.dart
flutter/lib/features/complaints/presentation/pages/complaints_page.dart
flutter/lib/features/complaints/presentation/widgets/complaint_list_card.dart
flutter/lib/features/complaints/presentation/widgets/status_badge.dart
flutter/lib/features/notifications/presentation/pages/notifications_page.dart
flutter/lib/features/notifications/presentation/widgets/notification_card.dart
```

(بالإضافة لملفات الرئيسية وCreate Complaint المعدَّلة قبل هذا الجزء من الجلسة: `home_page.dart`, `category_chip.dart`, `create_complaint_page.dart`, وملفات الـl10n.)

كل التعديلات دُفعت فعليًا لجهازك ومتأكد منها بـ`git status --short` بعد كل رفعة.

---

## 10. الخلاصة

الشاشات الخمس الأساسية (الرئيسية، قائمة الشكاوى، تفاصيل الشكوى، الإشعارات، والشريط السفلي المشترك) بقت **مطابقة بدقة عالية** لأحدث تصميم Figma من ناحية الألوان، الخطوط، المسافات، اتجاه RTL، وترتيب العناصر — مع الحفاظ الكامل على البنية المعمارية والمنطق التجاري الموجود. الملف الشخصي اتصحح جزئيًا (قائمة الإعدادات)، لكن نظام الرتب/النقاط الجديد يحتاج قرار منتج قبل البناء لأنه فيتشر كامل جديد مش مجرد مزامنة تصميم. أهم خطوتين تاليتين: (1) تشغيل `flutter analyze`/`flutter test` محليًا للتأكد النهائي، و(2) مراجعة قسم "المسائل المتبقية" مع فريق التصميم/المنتج لتحديد أي منها فعلاً في نطاق العمل الحالي.
